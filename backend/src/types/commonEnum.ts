export enum UserRole {
  RECRUITER = 1,
  CANDIDATE = 2,
}

export enum JobStatus {
  CLOSED = 0,
  DRAFT = 1,
  OPEN = 2,
}

export enum ApplicationStage {
  APPLIED = 1,
  SCREENING = 2,
  INTERVIEW = 3,
  OFFER = 4,
  REJECTED = 5,
}
